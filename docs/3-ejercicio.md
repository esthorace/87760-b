# Ejercicio 3

- Crear una app llamada "servicios"

- Implementar enrutamiento (`urls.py` en "servicios" y la función `include` en `config.urls`)

- Crear los templates necesarios para navegar entre `core` y `servicios`

- Crear el modelo `Servicio` con dos campos: `nombre` y `descripcion`, registrarlo en la app `admin`

- Crear superusuario, abrir app `admin` y realizar un CRUD con el modelo `Servicio`